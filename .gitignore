node_modules/
dist/
.DS_Store
*.log

# public/images/ (the Met Museum starter set) IS committed, as a stopgap so
# the live GitHub Pages deploy has something to show before Phase 3/4 wire up
# live multi-source fetching. images_captioned/ is an unused experimental
# variant (add_captions.py) — stays ignored, not referenced by public/images.json.
/images_captioned/

# Capacitor/Android build outputs (added once android/ exists)
android/app/build/
android/.gradle/
android/local.properties

# Local secrets (API keys for NASA/Smithsonian/Europeana/Rijksmuseum, Google OAuth client)
.env
.env.local
